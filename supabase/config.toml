[functions.calendar-feed]
verify_jwt = false

# Stripe calls this directly and has no knowledge of Supabase's JWT scheme --
# it authenticates the request itself via the Stripe-Signature header, which
# the function verifies against STRIPE_WEBHOOK_SECRET.
[functions.stripe-webhook]
verify_jwt = false